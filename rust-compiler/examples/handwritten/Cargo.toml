[package]
name = "handwritten"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Hand-written compiled forms of every EO test program — the shape eo-compile should eventually emit."

[[bin]]
name = "handwritten"
path = "src/main.rs"

[[bin]]
name = "bench"
path = "src/bin/bench.rs"

[lib]
path = "src/lib.rs"

[dependencies]
eo-rt = { path = "../../eo-rt" }
